#pragma once

// Copy this file to Secrets.h (gitignored - never commit real credentials)
// and fill in your own network. WifiSetup.cpp includes Secrets.h.

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"
